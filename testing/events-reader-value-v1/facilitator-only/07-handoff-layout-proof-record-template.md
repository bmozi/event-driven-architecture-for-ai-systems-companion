# Handoff Layout Proof Record Template

**Packet:** EVT-RV-PILOT-001 version 1.2.6
**Status:** Facilitator-only blank template; no layout result exists

After the completed Markdown handoff is manifested and verified, render its
exact bytes and export this record as
`EVT-A-HANDOFF-LAYOUT-PROOF-<attempt-id>-v1.md`. Preserve the PDF separately
as `EVT-A-ONE-SCREEN-HANDOFF-v1.pdf`.

## Exact identity and reproducible evidence

- Attempt ID:
- Markdown filename/SHA-256: `EVT-A-ONE-SCREEN-HANDOFF-v1.md` /
- PDF filename/SHA-256: `EVT-A-ONE-SCREEN-HANDOFF-v1.pdf` /
- Proof filename: `EVT-A-HANDOFF-LAYOUT-PROOF-<attempt-id>-v1.md`
- Artifact ID/version: `EVT-A-HANDOFF-LAYOUT-PROOF/v1`
- Literal rendering command:
- Renderer and exact version; supporting tool versions:
- Page size: `US Letter portrait` / invalid
- Page count: `1` / invalid
- Top/right/bottom/left margins in inches:
- Minimum body and table text size in points:
- Reader-facing word count:
- Immutable provenance words excluded from reader-facing count:
- Clipping, overlap, hidden overflow, or unreadable shrinking observed:
- Visual inspection actor and timestamp/timezone:
- Layout result: `PASS` / `FAIL`
- Completion timestamp/timezone:
- State: `LAYOUT PROOF COMPLETE` / invalid

`PASS` requires exactly one US Letter portrait page, every margin at least
0.5 inch, all body and table text at least 9 points, no more than 450
reader-facing words excluding only the handoff's explicitly labeled immutable
provenance sections, and no clipping, overlap, hidden overflow, or unreadable
shrinking. Preserve a two-page or compressed result as `FAIL`; its one-page
disposition is `HOLD`. Missing proof also means no favorable one-page claim.

Layout proof is not comprehension evidence. A local PASS does not establish
that a person can scan, understand, trust, or use the handoff.
